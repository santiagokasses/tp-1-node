const PI = 3.14

const suma = (num1, num2) => (num1 + num2)
const resta = (num1, num2) => (num1 - num2)

function multxPI(num1){
    return num1*PI
}

module.exports = {
    suma,
    resta,
    multxPI,
    PI
}
